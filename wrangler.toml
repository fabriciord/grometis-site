name = "grometis-site"
compatibility_date = "2024-11-06"

pages_build_output_dir = "dist/static"

# Nota: Domínios customizados devem ser configurados via dashboard
# https://dash.cloudflare.com -> Pages -> grometis-site -> Custom domains
